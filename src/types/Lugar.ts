// Interface que define la estructura de un Lugar
// Incluye campos obligatorios que representan un lugar en el sistema
export interface Lugar{
    id_lugar: number;
    nombre: string;
    estado: string;
}